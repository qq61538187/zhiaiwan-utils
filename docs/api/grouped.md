# Grouped Exports

### array
<a id="array"></a>

The array method group.

#### Since

`+0.1.0`

#### Returns

*(object)*: `{ chunk, unique, difference, flatten, flattenDeep, intersection, union }`

#### Example

```ts
import { array } from '@zhiaiwan/utils'
// => undefined
```

### func
<a id="func"></a>

The function method group.

#### Since

`+0.1.0`

#### Returns

*(object)*: `{ debounce, once, memoize, throttle, curry }`

#### Example

```ts
import { func } from '@zhiaiwan/utils'
// => undefined
```

### util
<a id="util"></a>

The util method group.

#### Since

`+0.1.0`

#### Returns

*(object)*: `{ attempt, bindAll, cond, conforms, constant, defaultTo, flow, flowRight, identity, iteratee, matches, matchesProperty, method, methodOf, mixin, noConflict, noop, nthArg, over, overEvery, overSome, property, propertyOf, range, rangeRight, runInContext, stubArray, stubFalse, stubObject, stubString, stubTrue, times, toPath, uniqueId }`

#### Example

```ts
import { util } from '@zhiaiwan/utils'
// => undefined
```

### collection
<a id="collection"></a>

The collection method group.

#### Since

`+0.1.0`

#### Returns

*(object)*: `{ groupBy, keyBy, map, filter, reduce }`

#### Example

```ts
import { collection } from '@zhiaiwan/utils'
// => undefined
```

### string
<a id="string"></a>

The string method group.

#### Since

`+0.1.0`

#### Returns

*(object)*: `{ camelCase, kebabCase, snakeCase, capitalize, trim }`

#### Example

```ts
import { string } from '@zhiaiwan/utils'
// => undefined
```

### lang
<a id="lang"></a>

The language helper method group.

#### Since

`+0.1.0`

#### Returns

*(object)*: `{ isNil, isEmpty, isPlainObject, isNumber, isString }`

#### Example

```ts
import { lang } from '@zhiaiwan/utils'
// => undefined
```

### object
<a id="object"></a>

The object method group.

#### Since

`+0.1.0`

#### Returns

*(object)*: `{ pick, get, set, omit, has }`

#### Example

```ts
import { object } from '@zhiaiwan/utils'
// => undefined
```

### math
<a id="math"></a>

The math method group.

#### Since

`+0.1.0`

#### Returns

*(object)*: `{ add }`

#### Example

```ts
import { math } from '@zhiaiwan/utils'
// => undefined
```
