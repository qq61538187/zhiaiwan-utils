/**
 * 检查 `value` 是否被分类为一个 `Array` 对象。
 *
 * @static
 * @category Lang
 * @param {*} value 要检查的值。
 * @returns {boolean} 如果 `value` 是数组则返回 `true`，否则返回 `false`。
 * @example
 *
 */
var isArray = Array.isArray;
