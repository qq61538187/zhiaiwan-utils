/**
 * Returns a new empty array.
 *
 * @since +0.1.0
 * @category Util
 * @param {never} _ This method accepts no arguments.
 * @returns {never[]} Returns the new empty array.
 * @example
 *
 * stubArray()
 * // => []
 */
export function stubArray(): never[] {
	return [];
}

export default stubArray;
